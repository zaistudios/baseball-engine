node_modules/
dist/
*.log
.DS_Store

# One-off measurement scripts. scripts/balance.ts is the one that is KEPT and
# re-run; anything underscore-prefixed is a question somebody asked once, and
# the answer lives in the README or in the constant's own comment rather than
# in the script that produced it.
scripts/_*.ts
