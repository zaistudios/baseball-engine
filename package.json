{
  "name": "baseball-engine",
  "version": "1.14.2",
  "private": true,
  "type": "module",
  "description": "Basedball — an interactive, customizable baseball sim. Personal project.",
  "//": "dev and build are BASEDBALL (game.html). The roguelike in src/web is frozen: excluded from tsconfig and vitest, and no script builds index.html. See the note in tsconfig.json.",
  "scripts": {
    "play": "node src/cli/play.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage --coverage.reporter=text --coverage.include=src/**/*.ts --coverage.exclude=src/**/__tests__/**",
    "check": "npm run typecheck && npm run test",
    "dev": "vite --open /game.html",
    "build": "vite build --config vite.game.config.ts",
    "game": "vite --open /game.html",
    "sim": "node scripts/balance.ts",
    "export": "npm run check && vite build --config vite.game.config.ts && node scripts/bundle.mjs game",
    "release": "npm run export && node scripts/release.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
